/*
 * This is a program to convert back and forth between and Uint8Array 
 * and a unicode character using either UTF-8 or UTF-16 in order to save 
 * bandwidth during node.js socket.io emissions
 */

function bin_to_unicode(uint8array) {

}

function unicode_to_bin(unicode) {
    return unicode;
}
